# 解説HTML一覧サイト 要件定義

## 1. 目的

Claudeに作ってもらった解説HTMLを、後から見つけて読み返せる状態にする。
勉強した内容を復習するためのインフラであり、蓄積するほど価値が上がることを狙う。

## 2. 成功条件

「あの説明どこだっけ」と思ったときに、30秒以内に該当のHTMLを開ける。

これ以外を評価軸にしない。見た目の統一や分類の精緻さは成功条件ではない。

## 3. 最重要要件

**手でindexを編集する工程をゼロにする。**

「HTMLを置く → push → 一覧に載る」以外の手順を1つも挟まない。
手作業が1ステップでも入ると運用が止まるため、ここは他の要件に優先する。

## 4. スコープ

### v1でやること

- `docs/` 配下のHTMLを自動走査して一覧ページを生成
- 各ファイルのタイトルと最終更新日を表示
- 更新日の新しい順に並べる
- pushをトリガーに自動ビルド＋自動デプロイ
- スマートフォン縦持ちで読める

### v1でやらないこと

以下はすべて後から追加可能。最初は作らない。

- タグ・カテゴリによる分類
- 全文検索
- 各HTMLの見た目の統一
- 閲覧履歴、復習リマインド
- HTMLの自動取得（ダウンロード→配置は手作業でよい）

## 5. 前提

| 項目 | 内容 |
|---|---|
| リポジトリ | public |
| GitHubユーザー名 | inbry |
| 初期コンテンツ | 10本程度 |
| 入手方法 | Claudeのartifactを `.html` でダウンロード |
| 閲覧環境 | 主にiPhone、副次的にPC |
| 外部API | 使わない（課金なし） |

## 6. 運用フロー

1. Claudeのartifactを `.html` でダウンロード
2. `docs/` に移動する
3. commit & push
4. GitHub Actionsが一覧を再生成してデプロイ

ファイル名はリネームしない。日本語・スペースが含まれていてもよい（リンク生成時にURLエンコードする）。
命名規則は定めない。規則を設けると配置のたびに判断が発生し、運用が続かなくなるため。

## 7. 機能要件

| ID | 要件 |
|---|---|
| F1 | ビルド時に `docs/**/*.html` を再帰的に走査する |
| F2 | 各ファイルのタイトルを `<title>` → 最初の `<h1>` → ファイル名（拡張子なし）の優先順で決定する |
| F3 | 各ファイルの最終コミット日を `git log` から取得し、更新日とする |
| F4 | 更新日の降順で並べた一覧ページを生成する |
| F5 | 一覧の各項目はタイトルと更新日を表示し、タップで該当HTMLを開く |
| F6 | 生成物をGitHub Pagesにデプロイする |
| F7 | 生成される一覧ページ自身は走査対象から除外する |

## 8. 非機能要件

- レスポンシブ。iPhone縦持ちで横スクロールが発生しないこと
- 外部APIを呼ばない。ビルドはネットワーク非依存
- ビルド時間は1分以内
- 依存パッケージは最小限（標準ライブラリで完結することが望ましい）

## 9. 技術構成

- ビルドスクリプト: Python 3（標準ライブラリのみ。title抽出は `html.parser`、日付取得は `subprocess` 経由の `git log -1 --format=%cI -- <path>`）
- CI/CD: GitHub Actions
- ホスティング: GitHub Pages（Actions経由でデプロイ）

### 実装上の注意

- `actions/checkout` は `fetch-depth: 0` を指定する。デフォルトの浅いcloneでは `git log` からコミット日が正しく取れない
- コミット履歴のないファイル（新規追加でまだコミットされていない等）はファイルシステムの更新日時にフォールバックする
- Pagesのデプロイは `actions/upload-pages-artifact` + `actions/deploy-pages` を使い、生成物を `_site/` に出力する。`docs/` 配下を `_site/` にコピーした上で `_site/index.html` を生成する

## 10. ディレクトリ構成

```
.
├── docs/                 # HTMLを置く場所（手動配置）
│   ├── xxx.html
│   └── yyy.html
├── build.py              # 一覧生成スクリプト
├── README.md
└── .github/
    └── workflows/
        └── deploy.yml
```

## 11. README記載事項

以下を明記する。

> 本リポジトリは個人の学習メモであり、所属組織の見解を示すものではありません。

## 12. 受け入れ基準

- [ ] `docs/` にHTMLを1本追加してpushすると、手作業なしで一覧に反映される
- [ ] 一覧が更新日の新しい順に並んでいる
- [ ] `<title>` のないHTMLでも、見出しかファイル名でタイトルが表示される
- [ ] 日本語ファイル名のHTMLがリンクから正しく開く
- [ ] iPhoneで一覧を開いたとき、横スクロールなしで読める

## 13. 将来の拡張余地（v1では実装しない）

- 分類が必要になった場合は、各HTMLの `<head>` に `<meta name="tags" content="...">` を追加し、ビルド側で読む。既存ファイルの変更は不要
- 本数が増えて探しにくくなった場合は、ビルド時に全文を1つのJSONに出力し、クライアント側でフィルタする
